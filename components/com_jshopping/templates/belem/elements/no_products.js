import React, { useState } from '../../../js/react/node_modules/react';

const No_products = () => {
    const element = <p>{Joomla.JText._('COM_SMARTSHOP_NO_RELATED_PRODUCTS_AVAILABLE')}</p>;

    return (element);
}
export default No_products;
